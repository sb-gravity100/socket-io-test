tasks:
   - name: Server
     init: npm i
     command: npm run dev
   - name: Client
     init: cd client && npm i
     command: cd client && npm start
ports:
   - port: 5000
     visibility: public
     onOpen: ignore
   - port: 3000
     visibility: public
     onOpen: ignore
