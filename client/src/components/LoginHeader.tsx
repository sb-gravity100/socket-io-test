import { FC } from 'react';

const LoginHeader: FC = () => {
   return (
      <div className="fw-bold display-1 border-bottom border-dark">
         Za <span className="text-dark">Warudo</span>
      </div>
   );
};

export default LoginHeader;
